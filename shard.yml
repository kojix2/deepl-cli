name: deepl-cli
version: 0.1.4

targets:
  deepl:
    main: src/deepl.cr

dependencies:
  term-spinner:
    github: crystal-term/spinner
    branch: master
  http_proxy:
    github: mamantoha/http_proxy