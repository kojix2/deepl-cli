name: deepl-cli
version: 0.2.4

targets:
  deepl:
    main: src/cli.cr

dependencies:
  term-spinner:
    github: crystal-term/spinner
    branch: master
  http_proxy:
    github: mamantoha/http_proxy
